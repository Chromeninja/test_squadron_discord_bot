"""Deprecated test module for the removed GuildConfigService."""

import pytest

pytest.skip(
    "GuildConfigService has been retired; compatibility tests are intentionally disabled.",
    allow_module_level=True,
)
