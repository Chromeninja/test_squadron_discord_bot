# Discord Bot & Web Admin Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# Discord Bot Configuration
# ============================================================================

# Discord Bot Token (Required)
# Get from: https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# Config Path Override (Optional)
# Override default config.yaml location
# CONFIG_PATH=config/config.yaml

# Prefix Normalization Mode (Optional)
# Options: "enforce" (default) or "shadow" (log only, don't apply)
# PREFIX_NORMALIZATION_MODE=enforce

# Test Configuration (Optional - Test environment only)
# Set to 1 to enable dry run mode for testing (prevents bot login)
# TESTBOT_DRY_RUN=1

# ============================================================================
# Web Admin Dashboard Configuration
# ============================================================================

# Discord OAuth2 Configuration (Required for Web Admin)
# Get from: https://discord.com/developers/applications
DISCORD_CLIENT_ID=your_client_id_here
DISCORD_CLIENT_SECRET=your_client_secret_here
DISCORD_REDIRECT_URI=http://localhost:8081/auth/callback

# Discord Guild ID (Required for Web Admin)
# Your Discord server ID
DISCORD_GUILD_ID=your_guild_id_here

# Session Secret (Required for Web Admin)
# Generate a long random string (32+ characters) for production
SESSION_SECRET=dev_only_change_me_in_production

# Internal API Configuration (Optional - for bot<->backend communication)
# INTERNAL_API_URL=http://127.0.0.1:8082
# INTERNAL_API_KEY=your_internal_api_key_here

# Environment Mode (Optional - for internal API security)
# Set to "dev" or "test" to allow running without INTERNAL_API_KEY
# In production, leave unset or set to "production" and provide INTERNAL_API_KEY
# ENV=dev

# Bot Owner ID (Optional - for superuser access)
# BOT_OWNER_ID=your_discord_user_id_here

# Cookie Settings (Optional - defaults shown)
# COOKIE_SECURE=false  # Set to true in production with HTTPS
# COOKIE_SAMESITE=lax  # Options: strict, lax, none

# Frontend Configuration (Required for Web Admin)
# Backend URL for frontend API calls - must be set in web/frontend/.env
VITE_API_BASE=http://localhost:8081

# Frontend origin for backend CORS (Required for production)
# Add your production frontend URL here
FRONTEND_URL=http://localhost:5173

# Role Validation Cache TTL (Optional)
# ROLE_VALIDATION_TTL=30  # seconds

# ============================================================================
# Notes
# ============================================================================
# - Database path, logging level, rate limits, voice settings, and RSI config
#   are all configured in config/config.yaml (see config/config-example.yaml)
# - Only secrets and environment-specific URLs belong in this .env file
# - For production deployment, generate strong random values for SESSION_SECRET
#   and INTERNAL_API_KEY
